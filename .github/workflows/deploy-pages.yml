name: (Disabled) Deploy to GitHub Pages

# This workflow has been intentionally disabled. Original content removed.
# Reason: Removing automatic pages-build-deployment as requested.
# To restore, recover the previous version from git history.

on:
  workflow_dispatch: # Manual trigger only (no automatic events)

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "GitHub Pages deployment workflow disabled. No action performed." 
